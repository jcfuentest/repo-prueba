#include <stdio.h>
#include <conio.h>

int main() {

    int r,j, f[5]={1,2,3,4,60};

    for ( r = 0; r < 5; ++r) {
        printf("%i. digite un numero: ", 1+r);
        print("hola quepasa");
        scanf("%i", &f[r] );
        we
    }


    for ( j = 0; j < 5 ; ++j) {
        printf("%i", f[j]);
    }
    getch();

    return 0;
}
